/* Общие стили */
* {
  box-sizing: border-box;
}
html, body {
  overflow-x: hidden; /* Запрещаем горизонтальный скролл */
}
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background: #f7f7f7;
  color: #333;
  /* Оставляем body с колоночной раскладкой */
  display: flex;
  flex-direction: column;
}

/* Header */
header {
  background: #1d72b8;
  color: #fff;
  padding: 20px;
  text-align: center;
}
header h1 {
  margin: 0 0 10px;
}
#search-form {
  display: flex;
  justify-content: center;
  gap: 10px;
}
#search-form input[type="text"] {
  padding: 8px;
  width: 300px;
  border: none;
  border-radius: 4px;
}
#search-form button {
  padding: 8px 16px;
  border: none;
  border-radius: 4px;
  background: #fff;
  color: #1d72b8;
  cursor: pointer;
  font-weight: bold;
}

/* Фильтры */
#filters {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 10px;
  padding: 10px;
  background: #eaeaea;
}
#filters input,
#filters select {
  padding: 5px;
  border: 1px solid #ccc;
  border-radius: 4px;
  min-width: 80px;
}
#filters button {
  padding: 5px 10px;
  border: none;
  border-radius: 4px;
  background: #1d72b8;
  color: #fff;
  cursor: pointer;
}

/* Контейнер общего содержимого */
#main-content {
  width: 100%;
  /* На маленьких экранах элементы идут по очереди */
  display: block;
}

/* Контейнер результатов – убираем отступ слева, чтобы товары были до края */
#results {
  padding: 20px 20px 20px 0;
  margin: 0;
}

/* Заголовок магазина */
.store-heading {
  font-size: 1.3rem;
  margin: 20px 0 10px;
  color: #1d72b8;
  text-align: left;
}

/* Сетка карточек: строго 6 колонок на широких экранах, выравнено по левой стороне */
.product-grid {
  display: grid;
  grid-template-columns: repeat(6, 220px);
  gap: 20px;
  justify-content: start;
  margin: 0;
  padding: 0;
}

/* При узком экране адаптация без горизонтального скролла */
@media (max-width: 1340px) {
  .product-grid {
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }
}

/* Карточка товара */
.product-card {
  width: 220px;
  background: #fff;
  border: 1px solid #ddd;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  transition: transform 0.2s ease;
  display: flex;
  flex-direction: column;
}
.product-card:hover {
  transform: translateY(-5px);
}
.product-card img {
  display: block;
  margin: 0 auto; /* Центрирует картинку */
  width: 70%;
  height: 180px;
  object-fit: cover;
}

.product-card .card-content {
  padding: 10px;
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}
.product-card h3 {
  font-size: 1rem;
  margin: 0 0 8px;
  height: 2.2em;
  overflow: hidden;
}
.product-card p {
  margin: 4px 0;
  font-size: 0.9rem;
}
.product-card button {
  margin-top: 10px;
  padding: 8px;
  border: none;
  background: #1d72b8;
  color: #fff;
  border-radius: 4px;
  cursor: pointer;
  font-size: 0.9rem;
}

/* Блок навигации для магазина: кнопка "Вперед" сверху, "Назад" снизу */
.store-nav {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-top: 10px;
}
.store-nav button {
  padding: 5px 10px;
  border: none;
  border-radius: 4px;
  background: #1d72b8;
  color: #fff;
  cursor: pointer;
  font-size: 0.9rem;
}
.store-nav button:disabled {
  background: #ccc;
  cursor: not-allowed;
}

.cart-link-btn {
  display: inline-block;
  margin-top: 10px;
  padding: 8px 16px;
  background: #5cb85c;
  color: #fff;
  text-decoration: none;
  border-radius: 4px;
  font-weight: bold;
}
.cart-link-btn:hover {
  background: #4cae4c;
}


